import { MODULES, ModulePlaceholder } from "@/components/common/ModulePlaceholder";

export const metadata = { title: MODULES.leads!.title };

export default function LeadsPage() {
  return <ModulePlaceholder id="leads" />;
}
