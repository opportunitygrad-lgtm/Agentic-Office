import { MODULES, ModulePlaceholder } from "@/components/common/ModulePlaceholder";

export const metadata = { title: MODULES.analytics!.title };

export default function AnalyticsPage() {
  return <ModulePlaceholder id="analytics" />;
}
