import { MODULES, ModulePlaceholder } from "@/components/common/ModulePlaceholder";

export const metadata = { title: MODULES.marketing!.title };

export default function MarketingPage() {
  return <ModulePlaceholder id="marketing" />;
}
